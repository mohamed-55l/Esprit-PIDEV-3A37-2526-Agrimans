{% extends 'base.html.twig' %}

{% block title %}Edit Culture{% endblock %}

{% block body %}
    <div class="content-section form-section">
        <div class="page-header">
            <h1>Modifier la culture</h1>
            <div class="page-actions">
                <a href="{{ path('app_culture_index') }}" class="btn btn-secondary">Retour à la liste</a>
                <a href="{{ path('app_culture_new') }}" class="btn btn-primary">Nouvelle culture</a>
            </div>
        </div>

        {{ include('Parcelle/culture/_form.html.twig') }}
    </div>

    <div class="content-section">
        {{ include('Parcelle/culture/_delete_form.html.twig') }}
    </div>
{% endblock %}