{% extends 'base.html.twig' %}

{% block title %}Edit Parcelle{% endblock %}

{% block body %}
    <div class="content-section form-section">
        <div class="page-header">
            <h1>Modifier la parcelle</h1>
            <div class="page-actions">
                <a href="{{ path('app_parcelle_index') }}" class="btn btn-secondary">Retour à la liste</a>
                <a href="{{ path('app_parcelle_new') }}" class="btn btn-primary">Nouvelle parcelle</a>
            </div>
        </div>

        {{ include('Parcelle/_form.html.twig') }}
    </div>

    <div class="content-section">
        {{ include('Parcelle/_delete_form.html.twig') }}
    </div>
{% endblock %}